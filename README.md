# daanpay
Frontend Application For Daanpay

## Development

Install the project dependencies before starting the application:

```bash
npm install
```

The generated `node_modules/` directory is excluded from Git through the repository's `.gitignore` file.
